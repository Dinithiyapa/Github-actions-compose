update-linode:
  name: Update Linode Server
  needs:
    - build-web
    - build-api
  runs-on: ubuntu-latest
  steps:
    - name: Execute remote SSH commands
      uses: appleboy/ssh-action@master
      with:
        host: ${{ secrets.HOST }}
        username: ${{ secrets.USERNAME }}
        password: ${{ secrets.PASSWORD }}
        port: ${{ secrets.PORT }}
        script: |
          # Ensure repository is cloned on the remote server
          if [ ! -d "$GITHUB_WORKSPACE" ]; then
            git clone https://github.com/your-username/your-repo.git $GITHUB_WORKSPACE
          fi
          
          # Navigate to the correct directory
          cd $GITHUB_WORKSPACE
          
          # Check if Docker is installed, and install if missing
          if ! command -v docker > /dev/null 2>&1; then
            echo "Docker not found. Installing..."
            sudo apt-get update
            sudo apt-get install -y ca-certificates curl gnupg
            sudo install -m 0755 -d /etc/apt/keyrings
            curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg
            sudo chmod a+r /etc/apt/keyrings/docker.gpg
            echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
            sudo apt-get update
            sudo apt-get install -y docker-ce docker-ce-cli containerd.io
          else
            echo "Docker is already installed."
          fi

          # Check if Docker Compose is installed, and install if missing
          if ! docker compose version > /dev/null 2>&1; then
            echo "Docker Compose not found. Installing..."
            mkdir -p ~/.docker/cli-plugins/
            curl -SL https://github.com/docker/compose/releases/download/v2.20.2/docker-compose-linux-x86_64 -o ~/.docker/cli-plugins/docker-compose
            chmod +x ~/.docker/cli-plugins/docker-compose
            docker compose version
          else
            echo "Docker Compose is already installed."
          fi

          # Verify if docker-compose.yml exists in the directory
          if [ -f "$GITHUB_WORKSPACE/docker-compose.yml" ]; then
            echo "docker-compose.yml found at: $GITHUB_WORKSPACE/docker-compose.yml"
            docker compose pull
            docker compose up -d --remove-orphans
          else
            echo "docker-compose.yml not found. Exiting."
            exit 1
          fi
